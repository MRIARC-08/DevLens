// Phase 3: Graph data formatting
// Converts DB data → React Flow format (nodes + edges + layout)

export async function getGraphData(repoId: string) {
  // TODO: Phase 3
  // Returns: { nodes: Node[], edges: Edge[] }
  throw new Error("Not implemented");
}

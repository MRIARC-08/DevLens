// Phase 4: Polls /api/repos/:id/status every 2s until READY or FAILED
"use client";
export function useRepoStatus(repoId: string) {
  // TODO: Phase 4
  return { status: null, statusMessage: null };
}
